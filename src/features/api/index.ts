/**
 * Public entry point for the API feature (OpenAPI client wiring).
 *
 * Prefer importing from here (`@/features/api`) instead of deep paths.
 */

export { configureApiClient } from '@/features/api/configure-api-client'
